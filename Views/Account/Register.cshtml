@model EliteSportsAcademy.ViewModel.Account.RegisterViewModel

@{
    ViewData["Title"] = "Sign Up";
    Layout = "~/Views/Shared/Account/_AuthLayout.cshtml";
}

<div class="container mt-5">
    <div class="text-center mb-4">
        <h1 class="display-5">Sign Up</h1>
    </div>
    <form id="registerForm" action="/Account/Register" method="post" class="space-y-4">
        @Html.AntiForgeryToken()
        <div class="row g-3">
            <!-- First Name -->
            <div class="col-md-6">
                <label for="FirstName" class="form-label">First Name</label>
                <input type="text" class="form-control" id="FirstName" name="FirstName" required />
                @Html.ValidationMessageFor(m => m.FirstName, "", new { @class = "text-danger" })
            </div>

            <!-- Last Name -->
            <div class="col-md-6">
                <label for="LastName" class="form-label">Last Name</label>
                <input type="text" class="form-control" id="LastName" name="LastName" required />
                @Html.ValidationMessageFor(m => m.LastName, "", new { @class = "text-danger" })
            </div>

            <!-- Username -->
            <div class="col-md-6">
                <label for="Username" class="form-label">Username</label>
                <input type="text" class="form-control" id="Username" name="Username" placeholder="Enter your username" required />
                @Html.ValidationMessageFor(m => m.Username, "", new { @class = "text-danger" })
            </div>

            <!-- Email -->
            <div class="col-md-6">
                <label for="email" class="form-label">Email</label>
                <input type="email" class="form-control" id="email" name="email" placeholder="Enter your email" required />
                @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
                <small class="text-danger"></small>
            </div>

            <!-- Password -->
            <div class="col-md-6">
                <label for="password" class="form-label">Password</label>
                <input type="password" class="form-control" id="password" name="password" placeholder="Enter your password"
                       required minlength="6" pattern="^(?=.*[A-Z])(?=.*\W).*$" />
                <small class="text-muted">Password must be at least 6 characters long and include one capital letter and one special character.</small>
                <small class="text-danger"></small>
                @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })
            </div>

            <!-- Confirm Password -->
            <div class="col-md-6">
                <label for="confirmPassword" class="form-label">Confirm Password</label>
                <input type="password" class="form-control" id="confirmPassword" name="confirmPassword" placeholder="Confirm your password"
                       required />
                <small class="text-danger"></small>
                @Html.ValidationMessageFor(m => m.ConfirmPassword, "", new { @class = "text-danger" })
            </div>

            <!-- Photo URL -->
            <div class="col-md-6">
                <label for="photoURL" class="form-label">Photo URL</label>
                <input type="url" class="form-control" id="photoURL" name="photoURL" placeholder="Enter photo URL" />
                <small class="text-danger"></small>
                @Html.ValidationMessageFor(m => m.PhotoURL, "", new { @class = "text-danger" })
            </div>

            <!-- Gender -->
            <div class="col-md-6">
                <label for="gender" class="form-label">Gender</label>
                <select class="form-select" id="gender" name="gender" required>
                    <option value="">Select Gender</option>
                    <option value="Male">Male</option>
                    <option value="Female">Female</option>
                    <option value="Other">Other</option>
                </select>
                @Html.ValidationMessageFor(m => m.Gender, "", new { @class = "text-danger" })
            </div>

            <!-- Phone Number -->
            <div class="col-md-6">
                <label for="phoneNumber" class="form-label">Phone Number</label>
                <input type="tel" class="form-control" id="phoneNumber" name="phoneNumber" placeholder="Enter phone number" required />
                <small class="text-danger"></small>
                @Html.ValidationMessageFor(m => m.PhoneNumber, "", new { @class = "text-danger" })
            </div>

            <!-- Address -->
            <div class="col-md-6">
                <label for="address" class="form-label">Address</label>
                <textarea class="form-control" id="address" name="address" rows="2" placeholder="Enter address" required></textarea>
                <small class="text-danger"></small>
                @Html.ValidationMessageFor(m => m.Address, "", new { @class = "text-danger" })
            </div>

            <!-- Agreement Checkbox -->
            <div class="mb-3 form-check">
                @* <input type="checkbox" class="form-check-input" id="IsAgree" name="IsAgree" required /> *@
                @Html.CheckBoxFor(m => m.IsAgree, new { @class = "form-check-input" })
                <label for="IsAgree" class="form-check-label">I agree to the terms and conditions</label>
                @Html.ValidationMessageFor(m => m.IsAgree, "", new { @class = "text-danger" })
            </div>
        </div>

        <!-- Submit Button -->
        <div class="mt-4 text-center">
            <button type="submit" class="btn btn-primary w-50">Sign Up</button>
        </div>

        <!-- Already have an account -->
        <div class="text-center mt-4">
            <p class="small text-muted">
                Already have an account?
                <a href="/Account/Login" class="text-decoration-none">Login</a>
            </p>
        </div>
    </form>
</div>
